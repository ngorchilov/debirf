# DEBIRF configuration file: this will be sourced by bash

# initial commented values indicate the defaults

# label for debirf system (for hostname and initrd)
DEBIRF_LABEL="debirf-minimal"

# where to build the debirf
#DEBIRF_BUILDD="./$DEBIRF_LABEL"

# the debirf root, used by plugins
#DEBIRF_ROOT="$DEBIRF_BUILDD/root"

# Debian mirror
#DEBIRF_MIRROR="http://mirrors.kernel.org/debian"

# what distribution should debirf be built from?
#DEBIRF_DISTRO="lenny"

# kernel, either a package name, or a path to a .deb
# in either case, it must be a package or deb for a complete kernel,
# not a metapackage.
#DEBIRF_KERNEL_PACKAGE="linux-image-2.6.22-3-686"

# proxy
#export http_proxy=http://localhost:3128/
